## What does this PR do?

<!-- One or two sentences. Link the issue it closes if applicable: "Closes #123" -->

## Test plan

- [ ] `pnpm test` passes
- [ ] `pnpm coverage` passes (≥90% threshold)
- [ ] `npx tsc --noEmit` passes
- [ ] `npx tsc --noEmit -p tsconfig.scripts.json` passes
- [ ] New behaviour is covered by tests (or explain why not)
