pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm format:check
    lint:
      run: pnpm lint
    typecheck:
      run: npx tsc --noEmit && npx tsc --noEmit -p tsconfig.scripts.json
